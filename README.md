# calcy
Calcy is a lightweight terminal calculator built from scratch in C. It provides an interactive REPL where users can enter arithmetic expressions such as `1+2*3` or `(10+5)/3` and instantly get results. The project focuses on expression parsing, operator precedence, input handling, error handling, and clean Unix-style CLI behavior.
